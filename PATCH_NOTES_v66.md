# Wormhole Patch Notes — Beta-0.0.66

- Fixed `GenAIOptedOut` and other custom tag searches in offline Following Only mode.
- Separated the language selector from the custom tag text field.
- English remains the default language filter without occupying the custom tag field.
- Custom tags now match independently from language, preventing English from satisfying every custom tag search.
- Matching custom tags receive the searched-tag highlight on followed-channel cards.
