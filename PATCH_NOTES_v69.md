# Wormhole Patch Notes — Beta-0.0.69

- “Exclude restricted chat” is now enabled by default.
- The filter now excludes emote-only channels in addition to followers-only and subscribers-only channels.
- Older saved presets without a chat-filter preference inherit the new enabled default.
- Users can still turn the filter off or clear it with the active-filter controls.
